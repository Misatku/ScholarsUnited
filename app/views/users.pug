extends layout
block content
  section.main-content
    .section-box
      h1 All Users

    .card-container
      each user in users
        .profile-card
          h3= user.fullName
          p= user.interests || 'No interests listed'
          a(href=`/users/${user._id}` class='btn btn-secondary') View Profile
